# Copy this to .env and fill in values.
# For local dev, you can symlink from parent: ln -s ../.env .env

# Required for bulk-job and other tasks that use the database
DATABASE_URL=

# Trigger.dev (optional - CLI may use its own config)
# TRIGGER_API_URL=
# TRIGGER_SECRET_KEY=
